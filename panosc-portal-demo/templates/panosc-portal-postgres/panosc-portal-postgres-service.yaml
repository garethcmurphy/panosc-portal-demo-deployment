apiVersion: v1
kind: Service
metadata:
  name: panosc-portal-postgres
  namespace: {{ .Values.panoscPortal.namespace }}
  labels:
    app: panosc-portal-postgres
spec:
  type: NodePort
  ports:
   - port: 5432
  selector:
   app: panosc-portal-postgres
